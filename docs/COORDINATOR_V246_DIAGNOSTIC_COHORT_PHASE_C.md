# v246 Development Diagnostic Cohort - Phase C

## Frozen Live

Artifacts: `/private/tmp/v246-diagnostic-run1`

- records: 30;
- independent companies: 29;
- Website: 23/30;
- Career: 20/30;
- verified Job List: 15/30;
- S7 Exact: 9/30.

The cohort is development-only and has zero LinkedIn job-ID overlap with
Fresh100 and the preceding 24-record diagnostic cohort.

## Causal Findings

The four records ending at S5 do not form one cluster:

- Cricut: first-party asset probe finds the SmartRecruiters board but does not
  reinsert it into selection;
- Agile Defense: the Lever tenant is assembled in inline JavaScript that the
  transport detector does not parse;
- Vasion: the Workable component chunk is not reached before budget exhaustion;
- AECOM: a weak-text first-party handoff and a very large SmartRecruiters
  inventory require different fixes.

No S5 implementation is authorized from this group.

Yum! Brands, County of Maui and Duke University Health System do form one
LinkedIn-official homepage alias rejection cluster. `.246` implements and
closes that contract in its separate Phase C report. Sofidel is excluded
because its legal-form tokenization and scheduling are a different path.

Other records remain external blocks, candidate-generation gaps, provider
singletons or verified inventory terminals. Stage labels and budget errors were
not used as root-cause clusters.

## Replay Integrity

The automatic 30-record replay stopped on Cass County because continuous replay
allowed a runtime-only WP Job Manager board to cross a real live restart
boundary. S6 then skipped four page/script GET requests, leaving tape entries
unconsumed.

Replaying the other 29 records produced:

- reproduced: 26;
- budget recovery: 2;
- mismatch: 1;
- fixture gap: 0.

The mismatch is Sweep's second posting: URL, provider, tenant and opening are
unchanged, but the provider evidence URL differs between stored-board live
state and first-party Career replay state.

Cass and Sweep are retained as separate replay-integrity debt. Neither observed
shape reaches the three-company implementation threshold, so this phase does
not weaken tape consumption or make runtime-only locators durable.
