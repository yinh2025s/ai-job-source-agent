# v242 Provider Identity Continuation - Phase C

## Result

The two-phase backend coordinator now permits only a durable, typed
`partial / COMPANY_IDENTITY_AMBIGUOUS` native-provider portfolio to continue
from S5 to S6. An unverified Job List remains hidden.

Runtime-only portfolio members are removed from checkpoint state. A retained
safe subset is always marked incomplete, so replay cannot turn a truncated
candidate set into a verified absence claim. Canonical Greenhouse and Ashby
tenant boards are replay-safe only on their official hosts, single-tenant
paths and query-free URLs.

## Focused Live

Artifact:

```text
/private/tmp/fresh4-v242-provider-identity-run1
```

- Caesars restored S1-S5 and entered Oracle S6.
- Oracle returned a complete three-record title-filtered inventory.
- The only exact title was in Las Vegas, not the target Reno location, so no
  opening was published.
- CHAMP published no unrelated Greenhouse Job List or opening.
- Fabric and Prophetic did not reproduce their historical search candidates in
  this run, so they provide no live continuation evidence.
- Same-version replay reproduced 4/4.

A second Caesars run did not reproduce the S5 candidate and ended at the
earlier request-budget boundary. It is search volatility, not contrary
continuation evidence.

## Decision

The S5-to-S6 orchestration deadlock is closed. The broader
Caesars/Fabric/Prophetic recall cluster is not closed because their
provider-owned employer evidence differs by provider and current payload.

No public Job List, Exact URL, cross-company URL or cross-tenant URL was added
by continuation alone.
